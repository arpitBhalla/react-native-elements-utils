"use strict";
var testHelper = require('../testHelper');
test('card transform test', function () {
    var _a = testHelper('cardTransform', 'cardTransform'), expected = _a.expected, actual = _a.actual;
    expect(expected).toEqual(actual);
});
